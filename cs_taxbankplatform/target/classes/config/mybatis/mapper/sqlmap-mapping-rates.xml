<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dcits.govsbu.sd.taxbankplatform.rates.mapper.RatesMapper">
	<!--mybatis ehcache缓存配置,以下两个<cache>标签二选一,第一个可以输出日志,第二个不输出日志 -->
	<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/> 
	<!-- <cache type="org.mybatis.caches.ehcache.EhcacheCache"/> -->
	
	<resultMap id="ratesMap" type="com.dcits.govsbu.sd.taxbankplatform.rates.model.RatesEntity">
		<id property="id" column="lr_id"/>
		<result property="ratesName" column="lr_name" />
		<result property="code" column="lr_code" />
		<result property="enabled" column="enabled"/>
	</resultMap>

	<!-- 搜索数据 -->
	<select id="queryListByPage" parameterType="map" resultMap="ratesMap" flushCache="true" useCache="true">
		SELECT *
		FROM tb_loan_rates
		<where>
			<if test="ratesName != null and ratesName != ''">
			lr_name LIKE CONCAT('%','${ratesName}','%' ) 
			</if>
			and enabled = 'Y'
		</where>
	</select>
	
	<!-- 插入数据 -->
	<insert id="insert" parameterType="com.dcits.govsbu.sd.taxbankplatform.rates.model.RatesEntity" useGeneratedKeys="true" keyProperty="lr_id">
		insert into tb_loan_rates (
			lr_id,
			lr_name,
			lr_code,
			enabled
			)   
		values (
		    #{id},
			#{ratesName},
			#{code},
			#{enabled}
			)  
	</insert>
	
	<!-- 查询数据，验证数据是否存在 -->
	<select id="findByName" parameterType="String" resultMap="ratesMap" flushCache="true" useCache="true">
		SELECT * 
		FROM tb_loan_rates
		where lr_name = #{name} and enabled = 'Y'
	</select>
	
	<!-- 删除数据（逻辑删除） -->
	<update id="delRates" parameterType="com.dcits.govsbu.sd.taxbankplatform.rates.model.RatesEntity"  flushCache="true">
		update tb_loan_rates
		set enabled = 'N'
		where lr_id = #{id}
	</update>
	<!-- 根据ID查找对象 add by baolong.hu -->
	<select id="findById" parameterType="String" resultMap="ratesMap" flushCache="true" useCache="true">
		select 
	    	lr_name,
			lr_code,
			enabled
	    from tb_loan_rates
	    where lr_id = #{id}
	</select>
</mapper>