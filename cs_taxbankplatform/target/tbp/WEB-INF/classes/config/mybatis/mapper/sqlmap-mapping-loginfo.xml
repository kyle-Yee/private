<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dcits.govsbu.sd.taxbankplatform.loginfo.mapper.LogInfoMapper">
	<!--mybatis ehcache缓存配置,以下两个<cache>标签二选一,第一个可以输出日志,第二个不输出日志 -->
	<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/> 
	<!-- <cache type="org.mybatis.caches.ehcache.EhcacheCache"/> -->

	<sql id="logInfoAttributes">
		l_id,
		u_id,
		l_account_name,
		l_operation,
		l_content,
		l_create_time
	</sql>

	<resultMap id="logInfoMap" type="com.dcits.govsbu.sd.taxbankplatform.loginfo.model.LogInfoEntity">
		<id property="id" column="l_id"/>
		<result property="userId" column="u_id" />
		<result property="userName" column="u_name" />
		<result property="accountName" column="l_account_name"/>
		<result property="operation" column="l_operation"/>
		<result property="content" column="l_content"/>
		<result property="createTime" column="l_create_time"/>
	</resultMap>
	
	<select id="queryListByPage" parameterType="map" resultMap="logInfoMap" flushCache="true" useCache="true">
		SELECT 
		l_id,
		t.u_id,
		l_account_name,
		l_operation,
		l_content,
		l_create_time,f.u_name
		FROM tb_log_info  t left join tb_user f on t.u_id = f.u_id
		<where>
			<if test="accountName != null and accountName != ''">
				l_account_name = #{accountName}
			</if>
		</where>
	</select>
	
	<insert id="insert" parameterType="com.dcits.govsbu.sd.taxbankplatform.loginfo.model.LogInfoEntity">
		insert ignore into tb_log_info (
			l_id,
			u_id,
			l_account_name,
			l_operation,
			l_content,
			l_create_time
			)
		values (
			#{id},
			#{userId},
			#{accountName},
			#{operation},
			#{content},
			now()
			)
	</insert>
	
	<delete id="clear7daysBefore">
		<![CDATA[  
       delete from tb_log_info where DATE_SUB(CURDATE(), INTERVAL 7 DAY) > date(l_create_time)
    	]]>
	</delete>
	
</mapper>