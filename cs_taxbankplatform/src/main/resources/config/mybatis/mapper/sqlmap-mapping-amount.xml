<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dcits.govsbu.sd.taxbankplatform.amount.mapper.AmountMapper">
	<!--mybatis ehcache缓存配置,以下两个<cache>标签二选一,第一个可以输出日志,第二个不输出日志 -->
	<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/> 
	<!-- <cache type="org.mybatis.caches.ehcache.EhcacheCache"/> -->
	
	<resultMap id="amountMap" type="com.dcits.govsbu.sd.taxbankplatform.amount.model.AmountEntity">
		<id property="id" column="la_id"/>
		<result property="amountName" column="la_name" />
		<result property="code" column="la_code" />
		<result property="enabled" column="enabled"/>
	</resultMap>

	<!-- 插入数据 -->
	<insert id="insert" parameterType="com.dcits.govsbu.sd.taxbankplatform.amount.model.AmountEntity">
		insert into tb_loan_amount (
			la_id,
			la_name,
			la_code,
			enabled
			)   
		values (
			#{id},
			#{amountName},
			#{code},
			#{enabled}
			)  
	</insert>
	
	<!-- 查询数据，验证数据是否存在 -->
	<select id="findByName" parameterType="String" resultMap="amountMap" flushCache="true" useCache="true">
		SELECT * 
		FROM tb_loan_amount
		where la_code = #{name} and enabled = 'Y'
	</select>
	
	<!-- 搜索数据 -->
	<select id="queryListByPage" parameterType="map" resultMap="amountMap" flushCache="true" useCache="true">
		SELECT *
		FROM tb_loan_amount
		<where>
			<if test="amountName != null and amountName != ''">
			la_name LIKE CONCAT('%','#{amountName}','%' ) 
			</if>
			and enabled = 'Y'
		</where>
	</select>
	
	<!-- 删除数据（逻辑删除） -->
	<update id="delAmount" parameterType="com.dcits.govsbu.sd.taxbankplatform.amount.model.AmountEntity"  flushCache="true">
		update tb_loan_amount
		set enabled = 'N'
		where la_id = #{id}
	</update>
	
	<!-- 根据ID查找对象 add by baolong.hu -->
	<select id="findById" parameterType="String" resultMap="amountMap" flushCache="true" useCache="true">
		select 
	    	la_name,
			la_code,
			enabled
	    from tb_loan_amount
	    where la_id = #{id}
	</select>
</mapper>