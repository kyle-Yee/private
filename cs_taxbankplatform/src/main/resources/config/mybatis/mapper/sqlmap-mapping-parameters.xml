<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dcits.govsbu.sd.taxbankplatform.tbpStatic.mapper.ParametersMapper">
	<!--mybatis ehcache缓存配置,以下两个<cache>标签二选一,第一个可以输出日志,第二个不输出日志 -->
	<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/> 
	<!-- <cache type="org.mybatis.caches.ehcache.EhcacheCache"/> -->
	
	<resultMap id="parametersMap" type="com.dcits.govsbu.sd.taxbankplatform.tbpStatic.model.ParametersEntity">
		<id property="id" column="p_id"/>
		<result property="code" column="p_code" />
		<result property="name" column="p_name" />
		<result property="value" column="p_value" />
		<result property="enabled" column="enabled"/>
		<result property="creatorid" column="creatorid"/>
		<result property="createtime" column="createtime"/>
		<result property="updatorid" column="updatorid"/>
		<result property="updatetime" column="updatetime"/>
	</resultMap>
	
	<!-- 搜索数据 -->
	<select id="queryListByPage" parameterType="map" resultMap="parametersMap" flushCache="true" useCache="true">
		SELECT *
		FROM tb_parameters
		<where>
			<if test="name != null and name != ''">
			p_name LIKE CONCAT('%','${name}','%' )
			</if>
		</where>
	</select>
	
	<!-- 根据CODE搜索数据 -->
	<select id="findByName" parameterType="String" resultMap="parametersMap" flushCache="true" useCache="true">
		SELECT *
		FROM tb_parameters
		where p_code = #{name} and enabled = 'Y'
	</select>
	
	<!-- 点击编辑按钮时候查询单条数据 -->
	<select id="findById" parameterType="String" resultMap="parametersMap" flushCache="true" useCache="true">
		SELECT * FROM tb_parameters where p_id = #{id}
	</select>
	
	<!-- 插入数据 -->
	<insert id="insert" parameterType="com.dcits.govsbu.sd.taxbankplatform.tbpStatic.model.ParametersEntity" useGeneratedKeys="true" keyProperty="p_id">
		insert into tb_parameters (
			p_id,p_code,p_name,p_value,enabled,creatorid,createtime,updatorid,updatetime
			)   
		values (
			#{id},
			#{code},
			#{name},
			#{value},
			#{enabled},
			#{creatorid},
			#{createtime},
			#{updatorid},
			#{updatetime}
			)  
	</insert>
	
	<!-- 更新数据 -->
	<update id="update" parameterType="com.dcits.govsbu.sd.taxbankplatform.tbpStatic.model.ParametersEntity" flushCache="true">
		update tb_parameters
		<set>
			<if test="value != null and value != ''">
				p_value = #{value},
			</if>
			<if test="code != null and code != ''">
				p_code = #{code},
			</if>
			<if test="name != null and name != ''">
				p_name = #{name},
			</if>
			<if test="enabled != null and enabled != ''">
				enabled = #{enabled},
			</if>
			updatorid = #{updatorid},
			updatetime = now()
		</set>
		where p_id = #{id}
	</update>
	
	<!-- 搜索ID检测code -->
	<select id="findCode" parameterType="String" resultMap="parametersMap" flushCache="true" useCache="true">
		SELECT p_id
		FROM tb_parameters
		where p_code = #{code} and enabled = 'Y'
	</select>
	
	<!-- 根据p_code查询p_value -->
	<select id="QueryValueByCode" parameterType="String" resultType="String" flushCache="true" useCache="true">
		SELECT p_value FROM tb_parameters WHERE p_code = #{code} and enabled = 'Y'
	</select>
	
	
</mapper>