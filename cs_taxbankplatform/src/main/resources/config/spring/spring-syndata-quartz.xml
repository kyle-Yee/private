<?xml version="1.0" encoding="UTF-8"?>
<beans default-lazy-init="true" default-autowire="default"
	xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:sca="http://www.springframework.org/schema/sca"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
     http://www.springframework.org/schema/beans/spring-beans.xsd
     http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd
     http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
     http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

	<bean id="DataSynchroJob" class="com.dcits.govsbu.sd.taxbankplatform.gxgsService.controller.ClientController" />

	<bean id="DataSynchroJobMethod" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<property name="targetObject" ref="DataSynchroJob" />
		<!-- 要执行的方法名称 -->
		<property name="targetMethod" value="getJsonStr" />
	</bean>
	
	<!-- ======================== 调度触发器 ======================== -->
	<bean id="DataSynchroFactoryBean" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail" ref="DataSynchroJobMethod" />
		<property name="cronExpression" value="0/20 * * * * ?"></property>
		<!-- <property name="cronExpression" value="0 0/1 * * * ?" />   每五秒触发一次  0/5 * * * * ? -->
	</bean>


	<!-- 简单的触发器 -->
	<bean id="simpleTimingJobTrigger" class="org.springframework.scheduling.quartz.SimpleTriggerFactoryBean">
		<property name="jobDetail"><ref bean="DataSynchroJobMethod" /></property>
		<!-- 以毫秒为单位，启动3秒钟后触发 -->
		<property name="startDelay" value="3000" />
		<!-- 每间隔10毫秒触发一次 -->
		<property name="repeatInterval" value="10000" />
	</bean>
	
	<!-- ****************数据统计定时任务************************* -->
	<bean id="StasticsJob" class="com.dcits.govsbu.sd.taxbankplatform.task.controller.DataStasticsTask" />
	<bean id="StasticsJobMethod" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<property name="targetObject">
			<ref bean="StasticsJob" />
		</property>
		<property name="targetMethod">  <!-- 要执行的方法名称 -->
			<value>executeDataStasticsTask</value> 
		</property>
	</bean>
	<bean id="StasticsJobFactoryBean" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail" ref="StasticsJobMethod"></property>
		<property name="cronExpression" value="0 0 0/1 * * ?"></property> <!-- 每一小时执行一次0 0 0/1 * * ? ?  每分钟0 */5 * * * ? -->
	</bean>
	<!-- ****************数据统计定时任务************************* -->
	
	<!-- ======================== 调度工厂 ======================== -->
	<bean id="SpringJobSchedulerFactoryBean" class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="triggers">
			<list>
				<ref bean="DataSynchroFactoryBean" />
				<!-- <ref bean="StasticsJobFactoryBean" /> -->
			</list>
		</property>
		<property name="quartzProperties">   
            <props>   
                <prop key="org.quartz.threadPool.class">org.quartz.simpl.SimpleThreadPool</prop>   
                <prop key="org.quartz.threadPool.threadCount">1</prop>   
                <prop key="org.quartz.threadPool.threadPriority">5</prop>           
            </props>   
        </property>  
	</bean>
</beans>